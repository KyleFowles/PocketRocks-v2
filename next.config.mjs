/* ============================================================
   FILE: next.config.mjs

   SCOPE:
   Clean Next.js config for app router.
   - Remove deprecated "eslint" config from next.config.
   ============================================================ */

const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
